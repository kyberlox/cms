/**
 * @typedef UseModelOptions
 *
 * @property {string | number | null} id
 * @property {boolean} autoFetch
 * @property {boolean} syncPagingParamsWithURL - Flag to determine if the paging information should be stored in the URL search parameters.
 * @property {Array<string>} searchFields
 * @property {number} page
 * @property {number} pageSize
 * @property {Array<any>} filters
 * @property {Record<string, string>} orderBy
 * @property {boolean} abortPreviousRequest
 */
