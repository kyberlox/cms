name: Deepsel Core - Tests

on:
  workflow_call:
  # main is covered by auto-publish.yml, which calls this via workflow_call
  push:
    branches: [ dev ]
    paths:
      - 'deepsel/**'
      - 'tests/**'
      - 'pyproject.toml'
      - 'Makefile'
      - '.github/workflows/test.yml'
  pull_request:
    branches: [ main, dev ]
    paths:
      - 'deepsel/**'
      - 'tests/**'
      - 'pyproject.toml'
      - 'Makefile'
      - '.github/workflows/test.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
    - uses: actions/checkout@v6
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        make install-dev

    - name: Lint with flake8
      run: make lint
    
    - name: Security check with bandit
      run: make security
    
    - name: Test with pytest
      run: make test

    - name: Build and check package
      run: make check-dist
    
